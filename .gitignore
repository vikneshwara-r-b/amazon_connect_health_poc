*.swp
__pycache__
.pytest_cache
.venv
*.egg-info

# CDK asset staging directory
.cdk.staging
cdk.out
.claude

# CDK context cache -- caches real per-account values (account ID, VPC ID,
# subnet IDs) from `cdk synth`/`deploy` lookups; regenerated automatically,
# should not be committed.
cdk.context.json
